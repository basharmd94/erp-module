product ERP on the Net

table casalshrc2
  caption "Zone by Sales Type"
  columns ztime,zutime,zid,xtype,xstate,xdiv,xdescdet,xcode,xemp,xname,zemail,zactive

  primary key zid, xstate,xdiv
  
  foreign key
        zid references zbusiness.zid
		zid,xstate references casalshrc1.zid,xstate
  end foreign key
  
end table